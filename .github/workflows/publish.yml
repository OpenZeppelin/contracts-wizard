name: Publish Packages

on:
  workflow_dispatch:

concurrency: version-or-publish-${{ github.ref }}

jobs:
  publish:
    name: Publish Packages
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    runs-on: ubuntu-latest
    environment: publish
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0 # To get all tags
          ref: ${{ github.ref }}
      - name: Set up environment
        uses: ./.github/actions/setup
      - name: Install npm 11 for OIDC trusted publishing
        run: npm install -g npm@11.12.1
      - name: Check for new packages
        id: check-packages
        run: |
          yarn workspaces --json info | node -e "
            const info = JSON.parse(JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')).data);
            for (const [name, meta] of Object.entries(info)) {
              const pkgJson = require('./' + meta.location + '/package.json');
              if (!pkgJson.private) console.log(name + ':' + meta.location);
            }
          " | while IFS=: read -r pkg dir; do
            if ! npm view "$pkg" version > /dev/null 2>&1; then
              echo "New package detected: $pkg ($dir)"
              echo "$dir" >> "$RUNNER_TEMP/new_packages.txt"
            else
              echo "Existing package: $pkg"
            fi
          done
          if [ -s "$RUNNER_TEMP/new_packages.txt" ]; then
            echo "::warning::New packages detected — will publish with NPM token first"
            echo "has_new_packages=true" >> "$GITHUB_OUTPUT"
          else
            echo "All packages exist on npm — using OIDC trusted publishing"
            echo "has_new_packages=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Create Prepare Release PR
        id: changesets
        uses: changesets/action@e0145edc7d9d8679003495b11f87bd8ef63c0cba # v1.5.3
        with:
          title: Prepare Release
          commit: Prepare Release
          version: npm run version
          commitMode: github-api
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Check changesets status
        if: steps.changesets.outputs.hasChangesets == 'true'
        run: |
          echo "Changesets found. Merge Prepare Release PR before publishing."
          exit 1
      - name: Publish new packages with token
        if: steps.changesets.outputs.hasChangesets == 'false' && steps.check-packages.outputs.has_new_packages == 'true'
        run: |
          while IFS= read -r dir; do
            echo "Publishing new package from $dir"
            npm publish --provenance --access public "$dir"
          done < "$RUNNER_TEMP/new_packages.txt"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Publish existing packages with OIDC
        if: steps.changesets.outputs.hasChangesets == 'false'
        run: npm run publish
        env:
          NPM_CONFIG_PROVENANCE: true
